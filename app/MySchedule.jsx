import { TabBar, Tab, Layout, Text } from '@ui-kitten/components';

const MySchedule = () => {
    return (
    <Layout style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text category='h1'>MySchedule</Text>
    </Layout>
  );
}

export default MySchedule;